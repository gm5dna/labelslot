[package]
name = "labelslot"
version = "0.1.0"
description = "Move a shipping label PDF to any position on an A4 label sheet or onto a 4x6 thermal label, without scaling it."
edition = "2021"

[build-dependencies]
tauri-build = "2"

[dependencies]
tauri = "2"
